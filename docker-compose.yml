services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: aureva
      POSTGRES_PASSWORD: aureva_dev_pw
      POSTGRES_DB: aureva_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: